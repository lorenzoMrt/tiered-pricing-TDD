import TieredPricing from "../src/TieredPricing";

describe("TODO", () => {
	test("TODO: rename later", () => {
		const pricing: TieredPricing = new TieredPricing(2);
		expect(pricing.total()).toBe(598);
	});

	test("TODO: rename later", () => {
		const pricing: TieredPricing = new TieredPricing(3);
		expect(pricing.total()).toBe(717);
	});
});
